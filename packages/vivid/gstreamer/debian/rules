#!/usr/bin/make -f

build:
	dh build --before configure
	./autogen.sh
	./configure
	make
	dh build --after build

clean:
	dh clean

binary-arch:
	dh binary-arch

binary-indep:
	dh binary-indep

binary: build binary-arch binary-indep
